#include <chrono>
#include "EvoAPI.hpp"

using namespace std;

int main(int, char**) {


    
    // EvoAPI api = EvoAPI(100, 100, 0);
    // api.load_file();
    // api.predict();
    // api.show_result();

    return 0;
}
